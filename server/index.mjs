import {createServer} from 'node:http';
import {createReadStream} from 'node:fs';
import {stat} from 'node:fs/promises';
import {resolve,extname,sep} from 'node:path';
import {leadMiddleware} from './leads.mjs';
const root=resolve('dist');const leads=leadMiddleware();const mime={'.html':'text/html; charset=utf-8','.js':'text/javascript','.css':'text/css','.json':'application/json','.png':'image/png','.jpg':'image/jpeg','.jpeg':'image/jpeg','.jfif':'image/jpeg','.svg':'image/svg+xml','.wav':'audio/wav','.pdf':'application/pdf','.ttf':'font/ttf','.glb':'model/gltf-binary'};
createServer(async(req,res)=>{if(req.url?.startsWith('/api/'))return leads(req,res,()=>{res.statusCode=404;res.end();});try{const path=decodeURIComponent(new URL(req.url,'http://localhost').pathname);let file=resolve(root,'.'+path);if(file!==root&&!file.startsWith(root+sep)){res.statusCode=403;return res.end();}let info=await stat(file).catch(()=>null);if(!info?.isFile()){if(extname(path)){res.statusCode=404;return res.end('Not found');}file=resolve(root,'index.html');info=await stat(file);}res.setHeader('Content-Type',mime[extname(file)]||'application/octet-stream');res.setHeader('X-Content-Type-Options','nosniff');res.setHeader('Accept-Ranges','bytes');const range=/^bytes=(\d+)-(\d*)$/.exec(req.headers.range||'');if(range){const start=Number(range[1]),end=range[2]?Math.min(Number(range[2]),info.size-1):info.size-1;if(start>end||start>=info.size){res.writeHead(416,{'Content-Range':`bytes */${info.size}`});return res.end();}res.writeHead(206,{'Content-Range':`bytes ${start}-${end}/${info.size}`,'Content-Length':end-start+1});createReadStream(file,{start,end}).pipe(res);}else{res.setHeader('Content-Length',info.size);createReadStream(file).pipe(res);}}catch{res.statusCode=500;res.end('Server error');}}).listen(Number(process.env.PORT||4173),'127.0.0.1',()=>console.log('Shattyq: http://127.0.0.1:'+(process.env.PORT||4173)));
