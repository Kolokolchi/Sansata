import {siteUrl} from '../lib/site';
import React,{lazy,Suspense,useEffect,useState} from 'react';
import {ArrowUpRight,ArrowLeft,Heart,Download,SlidersHorizontal,LayoutGrid,List,Share2,Check,Scale,X,Printer,Box,Search} from 'lucide-react';
import {Flat,ExperienceConfig,areaLabel,money,statusLabel} from '../lib/experience';
const Scene=lazy(()=>import('./SceneViewer').then(m=>({default:m.SceneViewer})));
interface Props{flats:Flat[];favorites:string[];toggleFavorite:(id:string)=>void;onConsult:(topic:string)=>void;config:ExperienceConfig}
export function PlanCard({flat:p,favorites,toggleFavorite,compare,onCompare}:{flat:Flat;favorites:string[];toggleFavorite:(id:string)=>void;compare?:string[];onCompare?:(id:string)=>void}){return <article className="plan-card"><div className="plan-top"><span>{p.rooms}-комнатная</span><button className={`icon ${favorites.includes(p.id)?'saved':''}`} aria-label={`${favorites.includes(p.id)?'Удалить из избранного':'В избранное'}: ${p.id}`} onClick={()=>toggleFavorite(p.id)}><Heart size={20} fill={favorites.includes(p.id)?'currentColor':'none'}/></button></div><a className="plan-image" href={siteUrl(`/flat/${p.id}`)}><img src={siteUrl(p.image)} loading="lazy" alt={`Планировка ${p.id}, ${p.rooms} комнаты, секция ${p.section}, этаж ${p.floor}`}/></a><div className="catalog-meta"><span>Секция {p.section} · {p.floor} этаж</span><strong>{areaLabel(p)}</strong><small>{statusLabel[p.status]}</small></div><div className="plan-bottom"><div><small>Вариант {p.id.split('-')[1]}</small><strong>{p.price===null?'Цена по запросу':money(p.price)}</strong></div><a className="icon" href={siteUrl(`/flat/${p.id}`)} aria-label={`Открыть ${p.id}`}><ArrowUpRight/></a></div>{onCompare&&<button className={`compare-button ${compare?.includes(p.id)?'active':''}`} onClick={()=>onCompare(p.id)}><Scale size={16}/>{compare?.includes(p.id)?'В сравнении':'Сравнить'}</button>}</article>;}
export function Catalog({flats,favorites,toggleFavorite,onConsult,config,onlyFavorites=false}:Props&{onlyFavorites?:boolean}){
 const q=new URLSearchParams(location.search);const [rooms,setRooms]=useState(q.get('rooms')?.split(',').map(Number).filter(n=>[2,3,4].includes(n))||[]);const [section,setSection]=useState(q.get('section')||'');const [floor,setFloor]=useState(q.get('floor')||'');const [minArea,setMinArea]=useState(q.get('minArea')||'');const [maxArea,setMaxArea]=useState(q.get('maxArea')||'');const [maxPrice,setMaxPrice]=useState(q.get('maxPrice')||'');const [sort,setSort]=useState(q.get('sort')||'rooms');const [view,setView]=useState('grid');const [compare,setCompare]=useState<string[]>([]);const [compareOpen,setCompareOpen]=useState(false);const [notice,setNotice]=useState('');
 useEffect(()=>{const url=new URL(location.href);const fields={rooms:rooms.join(','),section,floor,minArea,maxArea,maxPrice,sort};Object.entries(fields).forEach(([k,v])=>v?url.searchParams.set(k,v):url.searchParams.delete(k));history.replaceState(null,'',url);},[rooms,section,floor,minArea,maxArea,maxPrice,sort]);
 const displayed=flats.filter(p=>(!onlyFavorites||favorites.includes(p.id))&&(!rooms.length||rooms.includes(p.rooms))&&(!section||p.section===Number(section))&&(!floor||p.floor===Number(floor))&&(!minArea||(p.area!==null&&p.area>=Number(minArea)))&&(!maxArea||(p.area!==null&&p.area<=Number(maxArea)))&&(!maxPrice||(p.price!==null&&p.price<=Number(maxPrice)))).sort((a,b)=>{if(sort==='area')return(a.area??Infinity)-(b.area??Infinity);if(sort==='price')return(a.price??Infinity)-(b.price??Infinity);if(sort==='floor')return a.floor-b.floor;return a.rooms-b.rooms;});
 const reset=()=>{setRooms([]);setSection('');setFloor('');setMinArea('');setMaxArea('');setMaxPrice('');setSort('rooms');};const toggleCompare=(id:string)=>{setNotice('');setCompare(v=>{if(v.includes(id))return v.filter(x=>x!==id);if(v.length>=3){setNotice('Можно сравнить до трёх планировок. Уберите один вариант.');return v;}return[...v,id];});};
 return <div className="experience-page"><div className="page-heading"><span className="eyebrow">SHATTYQ / {onlyFavorites?'ИЗБРАННОЕ':'ВЫБОР КВАРТИРЫ'}</span><h1>{onlyFavorites?'Ваши избранные планировки':'Найдите свою квартиру'}</h1><p>Планы из официального каталога Shattyq. Этаж и секция указаны на чертежах; опубликованный вариант не означает свободную квартиру.</p></div><div className="catalog-switch"><a className="button outline" href={siteUrl("/visual")}><Box size={18}/> Выбрать на 3D-плане</a><a className="button outline" href={siteUrl(onlyFavorites?'/parametric-search':'/favorite')}><Heart size={18}/>{onlyFavorites?'Весь каталог':`Избранное (${favorites.length})`}</a></div><div className="catalog-layout"><aside className="filters"><h3><SlidersHorizontal size={20}/> Параметры</h3><label>Количество комнат</label><div className="room-options">{[2,3,4].map(n=><button key={n} aria-pressed={rooms.includes(n)} className={rooms.includes(n)?'active':''} onClick={()=>setRooms(v=>v.includes(n)?v.filter(x=>x!==n):[...v,n])}>{n}</button>)}</div><label htmlFor="section-filter">Секция</label><select id="section-filter" value={section} onChange={e=>setSection(e.target.value)}><option value="">Все секции</option>{[1,2].map(n=><option key={n} value={n}>Секция {n}</option>)}</select><label htmlFor="floor-filter">Этаж на чертеже</label><select id="floor-filter" value={floor} onChange={e=>setFloor(e.target.value)}><option value="">Все этажи</option>{[...new Set(flats.map(p=>p.floor))].sort((a,b)=>a-b).map(n=><option key={n} value={n}>{n} этаж</option>)}</select><label>Площадь, м²</label><div className="range-inputs"><input aria-label="Площадь от" type="number" min="0" placeholder="От" value={minArea} onChange={e=>setMinArea(e.target.value)}/><input aria-label="Площадь до" type="number" min="0" placeholder="До" value={maxArea} onChange={e=>setMaxArea(e.target.value)}/></div><small>≈ — сумма площадей помещений на чертеже. Варианты без оцифрованной площади скрываются при фильтрации.</small><label htmlFor="price-filter">Бюджет до, ₸</label><input id="price-filter" type="number" min="0" step="1000000" value={maxPrice} onChange={e=>setMaxPrice(e.target.value)} placeholder="Любой"/><small>Опубликованные цены пока отсутствуют. При заданном бюджете варианты без цены не отображаются.</small><button className="text-link" onClick={reset}><X size={15}/> Сбросить фильтры</button><button className="button blue" onClick={()=>onConsult('Подбор квартиры по параметрам')}>Помочь с выбором <ArrowUpRight size={16}/></button></aside><div className="catalog-results"><div className="catalog-results-head"><span role="status">Найдено: {displayed.length}</span><label><span className="sr-only">Сортировка</span><select value={sort} onChange={e=>setSort(e.target.value)}><option value="rooms">По комнатности</option><option value="area">По площади</option><option value="floor">По этажу</option><option value="price">По цене</option></select></label><div className="view-toggle"><button className={view==='grid'?'active':''} aria-label="Карточки" onClick={()=>setView('grid')}><LayoutGrid size={18}/></button><button className={view==='list'?'active':''} aria-label="Список" onClick={()=>setView('list')}><List size={18}/></button></div></div>{notice&&<p className="form-error" role="status">{notice}</p>}{displayed.length===0?<div className="empty"><Search/><h3>{onlyFavorites&&!favorites.length?'Пока ничего не сохранено':'Подходящих планировок нет'}</h3><p>{maxPrice?'Цены не загружены. Уберите бюджет или запросите подбор.':'Измените параметры или откройте полный каталог.'}</p><button className="button outline" onClick={reset}>Сбросить параметры</button>{onlyFavorites&&<a className="button blue" href={siteUrl("/parametric-search")}>Перейти в каталог</a>}</div>:view==='grid'?<div className="plan-grid">{displayed.map(p=><PlanCard key={p.id} flat={p} favorites={favorites} toggleFavorite={toggleFavorite} compare={compare} onCompare={toggleCompare}/>)}</div>:<div className="flat-table"><table><thead><tr><th>Планировка</th><th>Секция / этаж</th><th>Площадь</th><th>Стоимость</th><th>Сохранить</th></tr></thead><tbody>{displayed.map(p=><tr key={p.id}><td><a href={siteUrl(`/flat/${p.id}`)}>{p.rooms}-комнатная · {p.id.split('-')[1]}</a></td><td>{p.section} / {p.floor}</td><td>{areaLabel(p)}</td><td>{p.price===null?'По запросу':money(p.price)}</td><td><button className="icon" aria-label={`Избранное ${p.id}`} onClick={()=>toggleFavorite(p.id)}><Heart size={18} fill={favorites.includes(p.id)?'currentColor':'none'}/></button></td></tr>)}</tbody></table></div>}</div></div>{compare.length>0&&<div className="compare-bar"><Scale size={20}/><span>В сравнении: {compare.length} / 3</span><button className="button blue" onClick={()=>setCompareOpen(true)}>Сравнить</button><button className="icon" aria-label="Очистить сравнение" onClick={()=>{setCompare([]);setCompareOpen(false);}}><X size={18}/></button></div>}{compareOpen&&<Compare plans={flats.filter(p=>compare.includes(p.id))} close={()=>setCompareOpen(false)}/>}</div>;
}
function Compare({plans,close}:{plans:Flat[];close:()=>void}){const ref=React.useRef<HTMLDialogElement>(null);useEffect(()=>{ref.current?.showModal();},[]);return <dialog ref={ref} className="modal wide compare-modal" aria-label="Сравнение планировок" onCancel={close}><button className="icon modal-close" aria-label="Закрыть сравнение" onClick={close}><X/></button><h2>Сравните своё пространство</h2><div className="compare-table"><table><tbody><tr><th>План</th>{plans.map(p=><td key={p.id}><a href={siteUrl(`/flat/${p.id}`)}><img src={siteUrl(p.image)} alt={p.id}/></a></td>)}</tr>{[['Комнаты',(p:Flat)=>p.rooms],['Секция / этаж',(p:Flat)=>`${p.section} / ${p.floor}`],['Площадь',(p:Flat)=>areaLabel(p)],['Стоимость',(p:Flat)=>p.price===null?'По запросу':money(p.price)],['Наличие',(p:Flat)=>statusLabel[p.status]]].map(([title,fn])=><tr key={title as string}><th>{title as string}</th>{plans.map(p=><td key={p.id}>{(fn as (p:Flat)=>React.ReactNode)(p)}</td>)}</tr>)}</tbody></table></div></dialog>;}
export function FlatPage({flats,favorites,toggleFavorite,onConsult,config,id}:Props&{id:string}){const p=flats.find(v=>v.id===id);const [zoom,setZoom]=useState(false);const [show3d,setShow3d]=useState(false);const [shared,setShared]=useState('');if(!p)return <div className="experience-page empty"><h1>Планировка не найдена</h1><a className="button blue" href={siteUrl("/parametric-search")}>Вернуться в каталог</a></div>;return <div className="experience-page flat-page"><a className="text-link" href={siteUrl("/parametric-search")}><ArrowLeft size={17}/> Все планировки</a><div className="flat-page-head"><div><span className="eyebrow">SHATTYQ · ВАРИАНТ {p.id.split('-')[1]}</span><h1>{p.rooms}-комнатная квартира</h1></div><button className="button outline" onClick={()=>toggleFavorite(p.id)}><Heart size={18} fill={favorites.includes(p.id)?'currentColor':'none'}/>{favorites.includes(p.id)?'В избранном':'В избранное'}</button></div><div className="flat-detail-layout"><div><div className="tabs feature-tabs"><button className={!show3d?'active':''} onClick={()=>setShow3d(false)}>План квартиры</button><button className={show3d?'active':''} onClick={()=>setShow3d(true)}>Демонстрация 3D-тура</button></div>{show3d?<Suspense fallback={<div className="loading-panel">Загрузка 3D…</div>}><Scene config={config} section={0} floor={0} interior/></Suspense>:<div className={`large-plan ${zoom?'is-zoomed':''}`}><button onClick={()=>setZoom(!zoom)} aria-label={zoom?'Уменьшить план':'Увеличить план'}><img src={siteUrl(p.image)} alt={`Чертёж ${p.rooms}-комнатной квартиры Shattyq`}/></button></div>}<div className="flat-actions"><a className="text-link" href={siteUrl(p.image)} download={`Shattyq-${p.id}.jpg`}><Download size={17}/> Скачать план</a><button className="text-link" onClick={()=>window.print()}><Printer size={17}/> Печать / PDF</button><button className="text-link" onClick={async()=>{try{await navigator.clipboard.writeText(location.href);setShared('Ссылка скопирована');}catch{setShared('Ссылка: '+location.href);}}}><Share2 size={17}/> Поделиться</button></div>{shared&&<p role="status">{shared}</p>}</div><aside className="flat-summary"><span className="status-chip">{statusLabel[p.status]}</span><h2>{p.price===null?'Цена по запросу':money(p.price)}</h2><dl><div><dt>Комнат</dt><dd>{p.rooms}</dd></div><div><dt>Секция</dt><dd>{p.section}</dd></div><div><dt>Этаж на чертеже</dt><dd>{p.floor}</dd></div><div><dt>Площадь</dt><dd>{areaLabel(p)}</dd></div></dl>{p.areaKind==='calculated'&&p.area!==null&&<p className="muted-note">Ориентир: сумма площадей помещений с учётом указанного коэффициента лоджии. Договорную площадь подтвердит менеджер.</p>}<div className="feature-chips">{p.features.map(f=><span key={f}><Check size={13}/>{f}</span>)}</div><button className="button blue" onClick={()=>onConsult(`Планировка ${p.id}: ${p.rooms} комнаты, секция ${p.section}, этаж ${p.floor}`)}>Запросить стоимость <ArrowUpRight size={17}/></button><a className="button outline" href={siteUrl(`/mortgage${p.price?'?price='+p.price:''}`)}>Рассчитать платёж</a><p className="muted-note">Это опубликованная планировка, не подтверждение наличия конкретной квартиры.</p></aside></div><h2 className="related-title">Другие варианты</h2><div className="plan-grid">{flats.filter(v=>v.rooms===p.rooms&&v.id!==p.id).slice(0,3).map(v=><PlanCard key={v.id} flat={v} favorites={favorites} toggleFavorite={toggleFavorite}/>)}</div></div>;}
export function VisualSelector({flats,favorites,toggleFavorite,onConsult,config}:Props){const [section,setSection]=useState(1);const [floor,setFloor]=useState(7);const matches=flats.filter(p=>p.section===section&&p.floor===floor);return <div className="experience-page visual-page"><div className="page-heading"><span className="eyebrow">SHATTYQ / ВЫБОР НА 3D-ПЛАНЕ</span><h1>От дома — к вашей квартире.</h1><p>Нажмите на этаж в сцене или выберите его в панели. Планировки привязаны к секциям и этажам, указанным на оригинальных чертежах.</p></div><div className="visual-layout"><Suspense fallback={<div className="loading-panel">Загрузка 3D…</div>}><Scene config={config} section={section} floor={floor} onSelect={(s,f)=>{setSection(s);setFloor(f);}}/></Suspense><aside className="visual-sidebar"><h3>Выберите секцию</h3><div className="room-options">{[1,2].map(n=><button className={section===n?'active':''} key={n} aria-pressed={section===n} onClick={()=>setSection(n)}>{n}</button>)}</div><h3>Этаж</h3><div className="floor-options">{Array.from({length:9},(_,i)=>9-i).map(n=><button key={n} className={floor===n?'active':''} onClick={()=>setFloor(n)} aria-pressed={floor===n}>{n}<span>{flats.filter(p=>p.section===section&&p.floor===n).length||'—'}</span></button>)}</div><small>Число справа — опубликованные варианты планировок, не остатки продаж.</small><a className="text-link" href={siteUrl("/parametric-search")}>Поиск по параметрам <ArrowUpRight size={16}/></a></aside></div><div className="visual-result-title"><h2>Секция {section} · {floor} этаж</h2><span role="status">Опубликовано вариантов: {matches.length}</span></div>{matches.length?<div className="plan-grid">{matches.map(p=><PlanCard key={p.id} flat={p} favorites={favorites} toggleFavorite={toggleFavorite}/>)}</div>:<div className="empty"><h3>Планы этого этажа пока не опубликованы</h3><p>Выберите этаж с доступными чертежами или запросите информацию.</p><button className="button blue" onClick={()=>onConsult(`Секция ${section}, этаж ${floor}`)}>Запросить планировку <ArrowUpRight size={17}/></button></div>}</div>;}
